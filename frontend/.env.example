# URL of the FastAPI backend — no trailing slash
# Local dev: leave empty (Vite proxy handles /api → localhost:8000)
# Production: set to your deployed backend URL
VITE_API_URL=https://api.stocks.cavanpage.com
